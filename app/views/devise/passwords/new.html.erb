<h2>Forgot your password?</h2>

<%= semantic_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= devise_error_messages! %>

  <%= f.inputs do %>
  	<div class="clearfix">
		
		<%= f.input :email %>
	</div>
  <%end%>
  <div class="forgot_button">
  	<%= f.commit_button  :label=>"Send me reset password instructions" ,:button_html=>{:class=>"primary"} %>
  </div>

  <div>
<% end %>

